# import CardModel from "CardModel.graphql"

type Field {
  id: String
  name: String!
  isRequired: Boolean
  type: FieldType
  model: CardModel
}

enum FieldType {
  STRING,
  NUMBER,
  DATE,
  IMAGE
}
